# 数据库连接（SQLite 文件路径，相对项目根目录）
DATABASE_URL="file:./dev.db"

# NextAuth 签名密钥（生产环境请用 openssl rand -hex 32 生成）
NEXTAUTH_SECRET="change-me-to-a-random-64-char-hex-string"

# 应用 URL
NEXTAUTH_URL="http://localhost:3000"
