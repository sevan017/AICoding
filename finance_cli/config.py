"""
config.py — 全局常量配置

本文件存放整个项目共用的常量，修改这里的值会影响所有模块。
"""

# 预设分类（可以自由增删，列表中每个字符串就是一个分类）
CATEGORIES = ["餐饮", "交通", "购物", "娱乐", "居住", "其他"]

# SQLite 数据库文件名（应用启动时会在同目录下自动创建这个文件）
DB_NAME = "finance.db"
